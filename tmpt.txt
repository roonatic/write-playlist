<구상 - 플레이리스트 자동 글 업로드>

사용언어: python

#if 음악파일에 tag가 존재하면
eyed3을 통해 tag 속 가수, 곡명 호출

#else
음악파일에 tag가 존재하지 않으면
파일의 제목 호출해서 가수, 곡명 split으로 분리

공통적으로 가져오는 것: 재생시간(mutagen 사용 추측)
재생시간 -10초 전에 게시글 작성하도록 함

게시글 작성시에
제목 → 플레이리스트 순서(pNo) + 가수 + '-' + 곡명
내용 → 본인이 원하는 내용

GUI: python PyQt 또는 PySide
